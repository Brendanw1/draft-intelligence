# Optional: serve the data bundle from a remote origin (e.g. R2 public bucket)
# instead of the copy bundled in public/data. Leave unset for same-origin.
# NEXT_PUBLIC_DATA_BASE=https://<your-r2-public-domain>
