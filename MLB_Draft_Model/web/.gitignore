# deps / build
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# env — never commit credentials
.env
.env.*
!.env.example

# os
.DS_Store
.vercel
.env*

# Frontend data — generated by scripts/export_frontend_data.py, committed for same-origin serving
# R2 sync tooling is optional, not needed for Vercel deploy
